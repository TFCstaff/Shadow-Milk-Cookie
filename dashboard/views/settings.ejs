<!DOCTYPE html>
<html>
<head>
<title>Server Settings</title>
<style>
body { background:#0b0f1e; color:#00aaff; font-family:sans-serif; padding:50px;}
input, select { margin:5px; padding:5px;}
button { background:#001122; color:#00aaff; border:none; padding:10px 20px; border-radius:5px;}
</style>
</head>
<body>
<h1>Server Settings</h1>
<form action="/dashboard/settings" method="POST">
<label>
    <input type="checkbox" name="auto_dm" value="1" <% if(settings.auto_dm==1){ %>checked<% } %> >
    Automatically send new member applications
</label><br>

<label>
    Default Verification Template:
    <select name="default_template">
        <option value="">--Select Template--</option>
        <% templates.forEach(t=>{ %>
        <option value="<%= t.id %>" <% if(settings.default_template==t.id){ %>selected<% } %>><%= t.name %></option>
        <% }) %>
    </select>
</label><br>

<button type="submit">Save Settings</button>
</form>
<br>
<a href="/dashboard">Back to Dashboard</a>
</body>
</html>
